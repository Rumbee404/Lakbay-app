// LakbaySearch intentionally does not need custom JavaScript for its
// core features -- search, filtering, and analytics all work through
// plain HTML forms and full-page requests handled by Flask. This file
// is kept minimal on purpose rather than adding JS for its own sake.

document.addEventListener("DOMContentLoaded", function () {
    console.log("LakbaySearch loaded.");
});
